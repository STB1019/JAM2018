using System;

namespace Scripts.Game.RoomFactory.Visualizers
{
	/// <summary>
	/// This interface represents an object that contains all the visual meshes into the scene that belong to the room architecture.
	/// It only has to reference the different GameObjects, and NOT the room dimensions or other parameters; that is "IRoomShape"'s responsability.
	/// </summary>
	public interface IRoomVisualizer
	{
		
	}
}

